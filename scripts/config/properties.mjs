// Centre angles for each slice
export const sliceCentres = {
    carb: 30,
    protein: 150,
    fat: 270,
};

// Properties for all slices
export const sliceProps = {
    cx: 200,
    cy: 200,
    radius: { inner: 20, outer: 192.5 },
    sliceAngle: { len: 120, offset: 3 },
};
